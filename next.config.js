/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Não usar exportação estática - precisamos de API routes
  // output: 'export',
}

module.exports = nextConfig
